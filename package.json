{
  "name": "musicwinvale",
  "version": "1.0.0",
  "description": "Reproductor de música YouTube en TypeScript vanilla",
  "main": "public/app.js",
  "scripts": {
    "build": "npx tsc",
    "postinstall": "chmod +x node_modules/.bin/tsc || true",
    "start": "npm run build && npx http-server public -p 8000 -c-1 --cors",
    "dev": "npx http-server public -p 8080 -c-1 --cors"
  },
  "keywords": ["music", "youtube", "player", "typescript"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}